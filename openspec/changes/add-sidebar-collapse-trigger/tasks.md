## 1. Sidebar Behavior

- [x] 1.1 Configure the authenticated sidebar to use shadcn icon-collapse mode and add a visible desktop `SidebarTrigger`; verify with `cd frontend && pnpm test src/components/app-sidebar.test.tsx`.
- [x] 1.2 Preserve the existing mobile trigger behavior; verify with `cd frontend && pnpm test src/components/app-sidebar.test.tsx`.

## 2. Tests and Validation

- [x] 2.1 Add component tests covering the desktop collapse trigger rendering and interaction; verify with `cd frontend && pnpm test src/components/app-sidebar.test.tsx`.
- [x] 2.2 Run focused frontend checks for this change; verify with `cd frontend && pnpm lint && pnpm test src/components/app-sidebar.test.tsx && pnpm build`.
